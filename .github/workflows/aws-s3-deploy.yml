name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: "22cb043-pranneeth"
        AWS_ACCESS_KEY_ID: "AKIASSOWTDMBR4LOBNS6"
        AWS_SECRET_ACCESS_KEY: "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"
        AWS_REGION: 'ap-south-1'   # optional: defaults to us-east-1
        SOURCE_DIR: 'src'      # optional: defaults to entire repository
